%make dummy binarization if not white then black
function [img] = binarize(img_in)
    img = img_in;
    [x,y] = size(img);
    for i = 1:x
        for j = 1:y
            disp()
            if img(x,y)<255
                img(x,y)=0;
            end;
        end;
    end;
end

